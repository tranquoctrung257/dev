#include <bits/stdc++.h>
using namespace std;
const float pi=3.14;

int main(){
	double r; cin >> r;
	double C = 2 * pi * r;
    double A = pi * r * r;
	cout << fixed << setprecision(4) << C << " " << fixed << setprecision(4) << A << endl;
}