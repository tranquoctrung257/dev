#include <bits/stdc++.h>
using namespace std;

int main(){
    long long x;
    cin >> x;
    long long ax = (long long)pow(x,3) + (long long)3 * pow(x,2) + x + 1;
    cout << ax ;

    // hoac
    // long long kq = 1ll * x * x * x + 3ll * x * x + x +1;
    // cout << kq;
}